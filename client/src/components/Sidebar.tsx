import React from "react";
import { SidebarHeader, SidebarProvider } from "./ui/sidebar";

const Sidebar: React.FC = () => {
  // TODO: Continue the Account Sidebar implementation.

  return (
    <SidebarProvider>
      <SidebarHeader></SidebarHeader>
    </SidebarProvider>
  );
};

export default Sidebar;
